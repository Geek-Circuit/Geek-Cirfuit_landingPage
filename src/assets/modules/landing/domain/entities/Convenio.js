export class Convenio {
    constructor({ id, titulo, descripcion, imagen }) {
        this.id = id
        this.titulo = titulo
        this.descripcion = descripcion
        this.imagen = imagen
    }
}