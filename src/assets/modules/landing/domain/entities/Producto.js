export class Producto {
    constructor({ id, nombre, descripcion, imagen, precio }) {
        this.id = id
        this.nombre = nombre
        this.descripcion = descripcion
        this.imagen = imagen
        this.precio = precio
    }
}